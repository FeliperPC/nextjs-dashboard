export default function Page() {
  return <p>Dashboard Page</p>
}

// In nextJs routes are based in folders, each folder should have a page file containing what will be shown when this route is called